<%= provide(:title, "ผู้ใช้ทั้งหมด") %>

<div class="container">

  <%= gen_breadcrumb({
    "หน้าแรก" => root_path,
    "สมาชิกทั้งหมด" => "active"
    }) %>

  <div class="page-header">
    <h1>สมาชิก<small>ทั้งหมด</small>

      <div class="pull-right">
        <a href="<%= new_user_path %>" class="btn btn-info btn-fab" data-toggle="tooltip" data-placement="left" title="เพิ่มสมาชิกใหม่">
          <i class="fa fa-plus"></i>
        </a>
      </div>
      <!-- ./pull-right -->

    </h1>
  </div>
  <!-- ./page-header -->
</div>
<!-- ./container -->

<div class="table-responsive">
  <table class="table table-bordered table-hover">
    <thead>
      <tr>
        <th>รหัสสมาชิก</th>
        <th>เลขประจำตัวประชาชน</th>
        <th>ชื่อ นามสกุล</th>
        <th>บทบาท</th>
        <th>ตำแหน่ง</th>
        <th>อีเมล</th>
        <th>เบอร์โทรศัพท์</th>
        <th>Line ID</th>
        <th>เมนู</th>
      </tr>
    </thead>
    <tbody>
      <%= render @users %>
    </tbody>
  </table>
</div>
<!-- ./table-responsive -->

<%= will_paginate %>
